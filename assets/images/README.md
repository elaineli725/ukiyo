# Hero image placeholder

Place the user-provided hero image file at:

- `assets/images/hero-yugao-user.jpg`

If this file is not present, the app falls back to the configured external image URL in `data/mockData.js`.
